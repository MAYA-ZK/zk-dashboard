/** @type {import('next').NextConfig} */
const nextConfig = {
  /*
  redirects: () => [
    {
      source: '/',
      destination: '/dashboard',
      permanent: false,
    },
  ],
  */
}

module.exports = nextConfig
